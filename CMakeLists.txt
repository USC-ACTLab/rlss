cmake_minimum_required (VERSION 3.0)
set (CMAKE_CXX_STANDARD 14)
project (ACT_PATH_REPLANNING)
add_executable(main main.cpp)
target_include_directories(main PRIVATE lib)

find_package(nlopt)
find_package(armadillo)
find_package(gsl)
find_package(gslcblas)
find_package(stdc++fs)
add_library(geometry hyperplane.cpp vectoreuc.cpp)
add_library(obstacle obstacle.cpp)
add_library(traj curve.cpp trajectory.cpp bezier_integrate_2d_8pts.cpp)
add_library(csv lib/csv/CSVparser.cpp)
add_library(optimization optimization.cpp)
target_link_libraries(main geometry obstacle optimization traj csv nlopt_cxx armadillo gsl gslcblas stdc++fs)
